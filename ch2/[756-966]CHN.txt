【756】还有一个命令是 `r`

如果我现在移到某个特定的字符上

然后按下 `r`

它会接受另一个字符作为参数

然后把这个特定的字符替换成刚刚的另一个字符 # REVIEW

呃，我再讲几个编辑命令

我认为我刚刚提到过一个

就是你可以撤销你在 Vim 里做的编辑

在 Normal 模式下按 `u` 就可以做到

`u` 代表撤销（Undo），还算好记

所以我按很多次 `u`，就会撤销我所有的改动

然后，撤销的反面，当然是——重做（Redo）

Vim 中，此命令的按键绑定到了 `Ctrl`+`R`

【772】好，那么我要说的另一个编辑命令

就是复制粘贴，因为……

哦，有问题吗？

*就是，Undo 是这样吗……比如它是不是撤销掉*

*上次退出 Insert 模式前，插入的所有内容？*

*还是只是最后一个字符呢？*

啊——这问题问得好！

这位同学问的是：Undo 会撤销

从你进入 Insert 模式以后的所有输入吗？

还是最后一个字符？

呃，答案实际上更复杂一点儿

【781】